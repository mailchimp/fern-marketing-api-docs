# yaml-language-server: $schema=https://schema.buildwithfern.dev/docs-yml.json

instances:
  - url: mailchimp.docs.buildwithfern.com
    custom-domain:
      - mailchimp.ferndocs.com
      - preview.developer.mailchimp.com
    edit-this-page:
      github:
        owner: fern-support
        repo: mailchimp
        branch: main
      launch: dashboard

title: Mailchimp | Developer

page-actions:
  options:
    skills:
      title: Install the Mailchimp skills
      description: >-
        Agent skills that teach coding agents the parts of the Marketing API
        that are easy to get wrong. Install them into Claude Code, Cursor, or
        any agent that supports the Agent Skills specification.
      install-command: npx skills add https://mailchimp.ferndocs.com
      skills:
        - name: set-consent-correctly
          description: >-
            Record marketing consent from a signup form, checkout, or CRM sync
            without overwriting consent a contact already gave.
          url: https://mailchimp.ferndocs.com/.well-known/agent-skills/set-consent-correctly/SKILL.md
        - name: set-up-oauth
          description: >-
            Implement the Mailchimp OAuth 2 flow, validate the state parameter,
            and store access tokens per connected account.
          url: https://mailchimp.ferndocs.com/.well-known/agent-skills/set-up-oauth/SKILL.md
        - name: subscribe-to-webhooks
          description: >-
            Create a webhook, verify delivery signatures, and keep a handler
            idempotent without echoing your own writes back into the audience.
          url: https://mailchimp.ferndocs.com/.well-known/agent-skills/subscribe-to-webhooks/SKILL.md
metadata:
  og:site_name: Mailchimp Developer
  og:title: Mailchimp Developer Documentation
  og:description: >-
    Find all the Mailchimp API documentation and tools developers need to send
    marketing and transactional emails.
  og:locale: en_US
  twitter:title: Mailchimp Developer Documentation
  twitter:description: >-
    Find all the Mailchimp API documentation and tools developers need to send
    marketing and transactional emails.

agents:
  robots-txt: ./robots.txt

ai-search:
  location:
    - docs
  # GENERATED from fern/style-guide/ai-search-system-prompt.md — do not edit
  # here. Edit that file, then re-inline it: Fern's system-prompt accepts
  # inline strings only (no file paths).
  system-prompt: |
    Write every answer in Mailchimp's documentation voice, inside whatever
    structure and citation format you're already using.

    # Voice

    Three voice principles shape every answer.

    Plainspoken: value clarity above all. No hyperbole, no over-promising, no
    ornamental sentence, because a reader trying to finish a task pays for
    every one.

    Genuine, which in documentation means respect. Assume a competent engineer
    who hasn't used this particular API before. Don't condescend, don't explain
    what they already know, don't pad with encouragement. Warmth here means
    anticipating the reader's next question, not cheering them on.

    Translators, the principle that matters most. Demystify. Explain why the
    API works the way it does, not only what to type. When Mailchimp's API does
    something surprising, say so plainly and give the reason; that is how these
    docs earn trust.

    Never clever at the reader's expense, chatty, padded, or salesy. Marketing
    claims never belong in a technical answer.

    # Register: match the question

    Choose one register per answer:

    - Orientation ("what does this API do," "where do I start," "what's
      next"): warm. Second person, contractions. Warmth comes from
      anticipating the reader's next question, not from personality.
    - Concepts and tasks (authentication, webhooks, pagination, batching, "how
      do I..."): plain, direct, explanatory. Answer why, not only how.
    - Reference (endpoints, parameters, fields, response shapes): dry and
      factual, even across a full breakdown. No "This parameter allows you
      to." Keep sibling items grammatically parallel.
    - Blocked reader (errors, troubleshooting, rate limits, quotas,
      deprecations): direct, action first. No reassurance, no hedging, no
      apologizing for a limit.

    Warmth that leaks into a reference or error answer reads as indifference.
    When in doubt, drop a register.

    # Answering a blocked reader

    Cover what happened, why it happened, and what to do, in that order.
    State causes as facts rather than possibilities. Give every limit as a
    numeral. Point at the machine-readable detail when the response carries
    one, such as a `detail` or `errors` field. Disambiguate adjacent codes
    when the reader may be debugging the wrong one: 400 against 422, 401
    against 403.

    # Rules for every answer

    Address the reader as "you." Reserve "we" for Mailchimp's own decisions
    and behavior.

    Use active voice and present tense. Use the imperative for instructions:
    "Copy the generated key," not "you should copy the generated key."

    Explain surprising behavior. If a developer would reasonably ask "wait,
    why?", answer it in one sentence before the code. When the API is
    genuinely confusing, say so plainly and explain the reason; that is
    expected of these docs, not an admission of failure.

    Don't explain what a competent engineer already knows, such as HTTP verbs,
    JSON, or OAuth in general.

    # Mechanics

    Sentence case for any heading. Serial commas. Numerals for numbers in
    technical contexts: "10 simultaneous connections," not "ten."

    Code formatting for parameters, values, endpoints, headers, and field
    names: `list_id`, `429`, `X-HTTP-Method-Override`. Placeholders in curly
    braces: `{list_id}`, `{subscriber_hash}`. Capitalize HTTP methods.

    Em dashes inside a sentence are closed up: `word—word`, never
    `word — word`. Prefer a semicolon for two related independent clauses and
    commas for a non-essential aside. One em dash per answer is plenty.

    Reproduce code samples from the docs rather than composing new ones. If
    you must adapt a sample, change only what the question requires.

    # Do not write like an AI

    These are hard constraints, not preferences.

    Never use: leverage, utilize, delve, robust, seamless, streamline,
    paradigm, landscape, realm, myriad, plethora, crucial, vital, pivotal,
    unlock (metaphorically), empower, elevate, supercharge, effortless,
    powerful. Write "use" for leverage and utilize, and "is" for "serves as,"
    "stands as," and "represents." Delete the magic adverbs: simply, merely,
    essentially, fundamentally, truly, significantly. "Simply" is the worst
    of them; if the step were simple the reader wouldn't be asking.

    Never use these transitions: "it's worth noting," "notably,"
    "importantly," "here's the thing," "let's dive in," "let's break this
    down," "think of it as," "in conclusion," "at the end of the day," "simply
    put."

    Never use negative parallelism ("It's not X—it's Y"), a rhetorical
    question you then answer ("Why does this matter? Because..."), three
    sentences in a row that open with the same words, or a false range ("from
    authentication to automation to analytics").

    No padded three-item lists; two or four items are often more honest. No
    fragment paragraphs for effect. No listicle in prose; use a real list. No
    emoji, arrows, or other Unicode decoration. No stakes inflation: a rate
    limit is a number, not a challenge to overcome.

    # Before you answer

    Check each of these and revise if any is wrong:

    1. Does the register match the question type, and did any warmth leak
       into a reference or error answer?
    2. Any banned word or transition? Check for simply, crucial, powerful,
       robust, seamless, leverage, "worth noting," "let's."
    3. Is every in-sentence em dash closed up, and would a semicolon or commas
       be better?
    4. For a blocked reader: what happened, why, and what to do, all stated as
       facts?
ai-examples:
  enabled: true

landing-page:
  page: Welcome
  path: docs/pages/landing-page.mdx
  slug: ""

layout:
  searchbar-placement: header
  page-width: 88rem
  content-width: 46rem
  tabs-placement: header

logo:
  light: ./assets/mc-logo-mobile-dark.svg
  dark: ./assets/mailchimp-logo-twotone-dark.svg
  height: 40
  href: /

favicon: docs/assets/favicon.png

typography:
  headingsFont:
    name: Means Web
    paths:
      - path: docs/assets/fonts/Means-Light-Web.woff2
        weight: 400
  bodyFont:
    name: Graphik Web
    paths:
      - path: docs/assets/fonts/Graphik-Regular-Web.woff2
        weight: 400
      - path: docs/assets/fonts/Graphik-Medium-Web.woff2
        weight: 500
  codeFont:
    name: Input Mono Regular
    path: docs/assets/fonts/Input-Mono-Regular.woff2

colors:
  accent-primary:
    dark: "#E7B75F"
    light: "#007C89"
  background:
    dark: "#001D28"
    light: "#EFEEEA"
  sidebar-background:
    dark: "#001D28"
    light: "#EFEEEA"
  header-background:
    dark: "#0C2832"
  card-background:
    dark: "#0C2832"
    light: "#FFFFFF"
  border:
    dark: "#18323B"

css: docs/assets/styles.css

js:
  - path: docs/assets/mc-onetrust-consent.js
    strategy: beforeInteractive
  - path: docs/assets/mc-gtm.js
    strategy: beforeInteractive
  - path: docs/assets/mc-sidebar-toc.js
    strategy: afterInteractive
  - path: docs/assets/mc-blog.js
    strategy: afterInteractive
  - path: docs/assets/mc-carousel.js
    strategy: afterInteractive
  - path: docs/assets/mc-header.js
    strategy: afterInteractive
  - path: docs/assets/mc-sidebar-product-title.js
    strategy: afterInteractive
  - path: docs/assets/mc-gtm-route-views.js
    strategy: afterInteractive

navbar-links:
  - type: minimal
    text: Tools
    url: /tools
  - type: minimal
    text: Release Notes
    url: /release-notes
  - type: filled
    text: Sign Up
    url: https://login.mailchimp.com/signup/

products:
  - display-name: Mailchimp Marketing API
    path: ./products/marketing-api.yml
    slug: marketing
    subtitle: Power timely, relevant marketing campaigns with custom data pulled directly from your app.
  - display-name: Mailchimp Transactional
    path: ./products/transactional.yml
    slug: transactional
    subtitle: Send targeted and event-driven messages to anyone, fast—with best-in-class deliverability.
  - display-name: Tools
    path: ./products/tools.yml
    slug: tools
    subtitle: Official client libraries and mobile SDKs for the Mailchimp APIs.
  - display-name: Release Notes
    path: ./products/release-notes.yml
    slug: release-notes
    subtitle: The latest updates, improvements, and fixes across the Mailchimp APIs.
  - display-name: Blog
    path: ./products/blog.yml
    slug: blog
    subtitle: Engineering stories and updates from the Mailchimp developer team.
  - display-name: Integration Partner Program
    path: ./products/integration-partner-program.yml
    slug: integration-partner-program
    subtitle: Grow your Mailchimp integration with exclusive partner benefits.

# Redirects for the 2026-08-21 Marketing API docs reorganization.
# See notes/proposed-ia.md for the full mapping.
redirects:
  # fundamentals.mdx was split across five destinations; API structure is the
  # closest successor and both landing pages linked here as "Documentation".
  - source: /marketing/docs/fundamentals
    destination: /marketing/api-concepts/api-structure
  - source: /marketing/docs/methods-parameters
    destination: /marketing/api-concepts/methods-and-parameters
  - source: /marketing/docs/errors
    destination: /marketing/api-concepts/errors
  - source: /marketing/docs/alternative-schemas
    destination: /marketing/api-concepts/alternative-schemas
  - source: /marketing/docs/audiences-introduction
    destination: /marketing/api-concepts/audiences-endpoints-beta
  - source: /marketing/docs/merge-fields
    destination: /marketing/concepts/audiences/merge-fields
  - source: /marketing/docs/e-commerce
    destination: /marketing/concepts/e-commerce
  # build-an-integration.mdx deleted 2026-08-24 (it was a table of contents
  # pointing at pages that cover each topic properly). Old URL now lands on
  # the OAuth app walkthrough, the closest live equivalent.
  - source: /marketing/docs/integrations
    destination: /marketing/build/start-developing/set-up-an-oauth-app
  - source: /marketing/docs/mobile-sdk
    destination: /marketing/build/start-developing/mobile-sdk
  - source: /marketing/docs/account-exports
    destination: /marketing/build/start-developing/account-exports

  # Guides tab dissolved. Mechanism guides moved to API core concepts.
  - source: /marketing/guides/quick-start
    destination: /marketing/build/start-developing/send-your-first-api-request
  # v6 auth reorg: the old guide WAS the implementation walkthrough, so it
  # retargets to the task page, not the concept page.
  - source: /marketing/guides/access-user-data-oauth-2
    destination: /marketing/build/start-developing/set-up-an-oauth-app
  - source: /marketing/guides/sync-audience-data-webhooks
    destination: /marketing/api-concepts/webhooks
  - source: /marketing/guides/run-async-requests-batch-endpoint
    destination: /marketing/api-concepts/batch-operations
  - source: /marketing/guides/organize-contacts-with-tags
    destination: /marketing/build/quickstarts/organize-contacts-with-tags
  - source: /marketing/guides/mobile-sdk
    destination: /marketing/build/start-developing/mobile-sdk

  # Concepts nested under Audiences to mirror the data model.
  - source: /marketing/concepts/merge-fields
    destination: /marketing/concepts/audiences/merge-fields
  - source: /marketing/concepts/tags-and-segments
    destination: /marketing/concepts/audiences/contacts
  - source: /marketing/concepts/audiences-and-contacts
    destination: /marketing/concepts/audiences
  # docs/merge-fields now lands in the nested location.
  # Deleted pages. create-audience -> the import/export quickstart that
  # replaces it (integrators build against existing audiences).
  - source: /marketing/guides/create-audience
    destination: /marketing/build/quickstarts/import-and-export-contacts
  # Also redirect the long-standing broken slug referenced from 4 pages.
  - source: /marketing/guides/create-your-first-audience
    destination: /marketing/build/quickstarts/import-and-export-contacts
  # Events guide deleted with no successor; endpoints remain in the reference.
  - source: /marketing/guides/track-outside-activity-events
    destination: /marketing
  # UI-configured feature, not API content; no successor page.
  - source: /marketing/guides/create-webhook-customer-journey-builder-campaign-manager
    destination: /marketing

  # Authentication reorganization, 2026-08-21 (v6). Authentication became a
  # section; OAuth 2 split into concept + task.
  - source: /marketing/api-concepts/oauth-2
    destination: /marketing/api-concepts/authentication/oauth-2

  # AI tools section slug shortened from the title-derived
  # `build-with-ai-tools` to `ai-tools`, 2026-09-04. The overview also moved
  # from a child page to the section landing page.
  - source: /marketing/build/build-with-ai-tools/ai-tools
    destination: /marketing/build/ai-tools
  - source: /marketing/build/build-with-ai-tools/mcp-server
    destination: /marketing/build/ai-tools/mcp-server
  - source: /marketing/build/build-with-ai-tools/agent-skills
    destination: /marketing/build/ai-tools/agent-skills
  - source: /marketing/build/build-with-ai-tools/llms-txt
    destination: /marketing/build/ai-tools/llms-txt

footer: ./components/Footer.tsx

theme:
  footer-nav: minimal
