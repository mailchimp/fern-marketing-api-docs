---
title: List carts returns the created and updated date
tags: ["Marketing API"]
---

[List carts](/marketing/api/ecommerce/list-store-carts) now returns the `created_at` and `updated_at` dates for each cart.

We had a bug where `created_at` and `updated_at` were blank strings for each cart in the list of carts.
