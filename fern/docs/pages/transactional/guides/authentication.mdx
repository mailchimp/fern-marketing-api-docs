# Authentication

All Mailchimp Transactional API requests require authentication via your API key.

## API Key

Include your API key in every request body as the `key` field.

```json
{
  "key": "your-api-key"
}
```

You can find your API key in your Mailchimp account settings under **Transactional > Settings > API Keys**.
