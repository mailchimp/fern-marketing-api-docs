# Templates

Templates let you store reusable email layouts on Mailchimp's servers and reference them by name when sending messages.

## Add a Template

`POST /templates/add`

## List Templates

`POST /templates/list`

## Render a Template

`POST /templates/render`
