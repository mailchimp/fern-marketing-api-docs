# Messages

Messages are the core resource of the Mailchimp Transactional API. Use the messages endpoints to send, query, and manage your transactional emails.

## Send a Message

`POST /messages/send`

## Send Using a Template

`POST /messages/send-template`

## Query Message Info

`POST /messages/info`
