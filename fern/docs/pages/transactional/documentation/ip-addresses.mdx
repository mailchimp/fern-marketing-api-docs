# IP Addresses

Manage dedicated IP addresses for your Mailchimp Transactional account to control your sending reputation.

## List IPs

`POST /ips/list`

## IP Info

`POST /ips/info`

## Provision an IP

`POST /ips/provision`
