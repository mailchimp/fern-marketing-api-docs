# Allowlists

Use allowlists to ensure that specific email addresses or domains are never added to your rejection list.

## Add to Allowlist

`POST /allowlists/add`

## List Allowlist Entries

`POST /allowlists/list`

## Delete from Allowlist

`POST /allowlists/delete`
