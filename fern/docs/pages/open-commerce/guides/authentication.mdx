# Authentication

Mailchimp Open Commerce uses token-based authentication for its GraphQL API.

## Obtain a Token

Send a `createAccountEmailRecord` mutation with your credentials to receive an access token.

## Use the Token

Include the token in the `Authorization` header of every request:

```
Authorization: Bearer <your-token>
```
