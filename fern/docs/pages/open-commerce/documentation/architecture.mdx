# Architecture

Open Commerce follows a microservices architecture with a central GraphQL API gateway.

## Services

- **API** — The core GraphQL API server
- **Admin UI** — A React-based admin interface
- **Storefront** — Your custom frontend application
- **MongoDB** — Primary data store
- **Hydra** — OAuth2 server for authentication
